import "./NavBar.css";

import MenuButton from "../MenuButton/MenuButton";
const NavBar = () => {
  return (
    <nav className="nav-bar-container">
      <MenuButton  />
    </nav>
  );
};

export default NavBar;
